services:
  backend:
    image: chatbot-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    image: chatbot-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    volumes:
      - ./nginx-local.conf:/etc/nginx/conf.d/default.conf:ro
